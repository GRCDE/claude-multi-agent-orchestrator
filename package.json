{
  "name": "claude-multiagents",
  "version": "4.2.0",
  "description": "Multi-Agent Orchestrator powered by Claude Code CLI",
  "main": "server.js",
  "type": "commonjs",
  "keywords": [
    "multi-agent",
    "claude",
    "orchestrator",
    "ai",
    "automation"
  ],
  "author": "choda",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "jest --verbose --runInBand --forceExit",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "docker:build": "docker build -t claude-multiagents .",
    "docker:run": "docker compose up -d"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "express-rate-limit": "^8.3.2",
    "swagger-ui-express": "^5.0.1",
    "winston": "^3.19.0",
    "ws": "^8.20.0"
  },
  "optionalDependencies": {
    "@anthropic-ai/claude-code": ">=1.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "playwright": "^1.59.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
